# ============================================================
# NoteFusion AI - Python Dependencies
# ============================================================

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0          # async PostgreSQL driver
aiosqlite>=0.19.0        # async SQLite (local dev / tests)

# Pydantic & Settings
pydantic>=2.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# Authentication & Security
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Rate Limiting
slowapi>=0.1.9

# HTTP Client (used by LLM factory)
httpx>=0.25.0

# Monitoring / Metrics
prometheus-fastapi-instrumentator>=6.1.0
sentry-sdk[fastapi]>=1.38.0

# Email
aiosmtplib>=3.0.0

# Redis (caching / pub-sub)
redis>=5.0.0
aioredis>=2.0.0

# AI / ML
openai>=1.3.0            # latest openai SDK

# Audio Processing
pydub>=0.25.1

# OCR (optional – install tesseract separately)
Pillow>=10.0.0
pytesseract>=0.3.10

# Export / Document Generation
jinja2>=3.1.0
markdown2>=2.4.10
weasyprint>=60.1

# Stripe Payments
stripe>=7.0.0

# Websockets
websockets>=12.0

# Structured Logging
structlog>=24.1.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
httpx>=0.25.0            # used by TestClient
anyio>=4.0.0

# Code Quality (dev only)
black>=23.0.0
isort>=5.12.0
